// const app = require('@root/app');
// const request = require('supertest')(app);

describe('Testing to see if Jest works', () => {
    it('It should work', () => {
        expect(1).toBe(1);
    });
});
